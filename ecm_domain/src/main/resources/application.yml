
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecm
    username: ecm
    password: ecm
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
    hibernate:
      ddl-auto: none
    show-sql: false
server:
  port: 8080